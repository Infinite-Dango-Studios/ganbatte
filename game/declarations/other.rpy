label other_declarations:
    ####################################
    # Screen positions
    ####################################

    init:
        $ char_left = Position(xpos=0.15, xanchor="left")
        $ char_right = Position(xpos=0.85, xanchor="right")

    return
